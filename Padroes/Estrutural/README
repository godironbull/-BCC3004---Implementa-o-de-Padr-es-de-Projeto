## Diagrama de Classes

![alt text](image.png)


## Explicação do Padrão de Projeto Proxy

O padrão de projeto Proxy é um padrão estrutural que fornece um substituto ou representante para outro objeto, controlando o acesso a ele. Ele é útil quando você deseja adicionar uma camada de controle sobre o acesso a um objeto, seja para fins de segurança, desempenho ou gerenciamento de recursos. A arquitetura do padrão Proxy geralmente envolve três componentes principais:

1. **Subject (Sujeito)**: Esta é uma interface comum que define os métodos que o RealSubject e o Proxy devem implementar. Isso garante que o Proxy possa ser usado no lugar do RealSubject sem que o cliente perceba a diferença.

2. **RealSubject (Sujeito Real)**: Esta é a classe que implementa a interface Subject e contém a lógica real do negócio. É o objeto que o Proxy irá representar e controlar o acesso.

3. **Proxy**: Esta é a classe que também implementa a interface Subject e contém uma referência ao RealSubject. O Proxy controla o acesso ao RealSubject, podendo adicionar funcionalidades adicionais, como autenticação, cache, ou registro de logs, antes ou depois de delegar as chamadas ao RealSubject.

### Problema que o Padrão Resolve

O problema resolvido pelo padrão de projeto Proxy é a falta de controle no acesso direto a um objeto. Este padrão introduz um intermediário que gerencia a comunicação entre o cliente e o objeto real